{
    "name":  "careerlaunch-studio",
    "version":  "0.1.0",
    "private":  true,
    "description":  "Original resume, CV, and cover-letter builder SaaS.",
    "scripts":  {
                    "dev":  "npm run dev --workspace @careerlaunch/web",
                    "build":  "npm run build --workspaces --if-present",
                    "postinstall":  "prisma generate && npx playwright install chromium 2>/dev/null || true",
                    "lint":  "npm run lint --workspaces --if-present",
                    "test":  "npm run test --workspaces --if-present",
                    "typecheck":  "npm run typecheck --workspaces --if-present"
                },
    "workspaces":  [
                       "apps/*",
                       "packages/*"
                   ],
    "engines":  {
                    "node":  ">=20"
                },
    "overrides":  {
                      "postcss":  "^8.5.10"
                  }
}
