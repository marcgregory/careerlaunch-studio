name: Keep Renderer Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-alive endpoint
        run: curl -sSf https://careerlaunch-studio.vercel.app/api/cron/keep-alive-renderer
